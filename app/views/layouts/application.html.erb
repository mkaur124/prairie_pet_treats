<!DOCTYPE html>
<html>
<head>
  <title><%= content_for(:title) || "Prairie Pet Treats" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= yield :head %>

  <link rel="icon" href="/icon.png" type="image/png">
  <link rel="icon" href="/icon.svg" type="image/svg+xml">
  <link rel="apple-touch-icon" href="/icon.png">

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload", media: "all" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

  <style>
    /* Navbar brand orange */
    .navbar-brand-orange {
      color: #ff8c69 !important;
      font-weight: bold;
    }

    /* Navbar links orange by default, red on hover */
    .navbar-nav .nav-link {
      color: #ff8c69 !important; /* orange default */
      font-weight: 600;
      transition: color 0.3s;
    }

    .navbar-nav .nav-link:hover {
      color: #ff4500 !important; /* red on hover */
    }

    /* Breadcrumbs styling */
    .breadcrumb {
      background-color: #fff3e0; /* soft cream background */
      padding: 8px 15px;
      border-radius: 8px;
      list-style: none;
      display: flex;
      gap: 5px;
      font-size: 0.95rem;
      margin-bottom: 20px;
      box-shadow: 0 3px 8px rgba(0,0,0,0.1);
    }

    .breadcrumb li {
      display: inline;
    }

    .breadcrumb li + li::before {
      content: "›"; /* arrow separator */
      margin: 0 5px;
      color: #ff8c69; /* orange arrow */
    }

    .breadcrumb a {
      text-decoration: none;
      color: #ff8c69;
      font-weight: 500;
    }

    .breadcrumb a:hover {
      color: #ff4500; /* red-orange on hover */
    }

    .breadcrumb .active {
      color: #333;
      font-weight: bold;
    }

    /* Table links black with no underline, hover in orange */
    .table a {
      color: black !important;
      text-decoration: none;
    }

    .table a:hover {
      color: #ff8c69 !important; /* orange on hover */
    }

    /* Background image for the whole site */
    body {
      background: url(<%= asset_path('smilingdog.jpg') %>) no-repeat center center fixed;
      background-size: cover;
    }

    /* Main container */
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 30px;
      background-color: rgba(255, 255, 255, 0.6); /* slightly transparent */
      border-radius: 15px;
      box-shadow: 0 8px 25px rgba(0,0,0,0.2);
    }

    /* Cards */
    .card {
      background-color: rgba(255, 255, 255, 0.3);
      border-radius: 15px;
      box-shadow: 0 6px 20px rgba(0,0,0,0.1);
    }

    /* Table container slightly transparent */
    .table-responsive {
      background-color: rgba(255, 255, 255, 0.4);
      border-radius: 15px;
      padding: 10px;
    }

    /* Headings */
    h1, h2, h3 {
      color: #ff8c69; /* orange headings */
    }
  </style>
</head>

<body>
  <!-- Flash messages -->
  <%= render "shared/flash" %>

  <!-- Navigation bar -->
  <%= render "shared/navbar" %>

  <!-- Page content -->
  <div class="container">
    <%= yield %>
  </div>

  <!-- Bootstrap JS bundle -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
